using Majors;

namespace Statistics
{
    public record MajorStatistics(MajorDetails Major, int Places, int AbiturientCount, int AgreementCount, double Contest, int AgreementPassingScore, int CurrentPassingScore);
}